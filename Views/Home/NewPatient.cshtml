@model Patient
@{
    ViewData["Title"] = "Detail Form";
}

<div class="container d-flex flex-column align-items-center">
    <h1>Add New Patient</h1>
    
    <form class="text-start w-50" asp-action="CreatePatient" , asp-controller="Home" method="post">
        @await Html.PartialAsync("_EditDetailForm")
        <div class="text-end">
            <button type="submit" class="btn btn-primary w-25">Save</button>
            <a href="/" class="btn btn-danger w-25">Exit</a>
        </div>
    </form>
</div>